echo "🔍 Running pre-push checks..."
echo ""

# Run all checks before allowing push
npm run check:build

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Pre-push checks failed! Please fix the errors above before pushing."
  exit 1
fi

echo ""
echo "✅ All checks passed! Proceeding with push..."

